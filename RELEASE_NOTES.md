## What's new in v0.0.12

- **Design:** an Apple Human Interface Guidelines pass — real, measured fixes rather than a restyle. The app now uses the actual macOS system font instead of a Windows font that silently fell back to generic sans-serif. Text contrast that measured below accessibility minimums (e.g. the About screen's section labels) is fixed. The accent color is now Apple's actual system blue.
